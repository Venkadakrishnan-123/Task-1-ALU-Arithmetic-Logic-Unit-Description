# Task-1-ALU-Arithmetic-Logic-Unit-Description
To design and simulate a 4-bit ALU (Arithmetic Logic Unit) that performs basic arithmetic and logical operations based on the selected operation code (opcode).
